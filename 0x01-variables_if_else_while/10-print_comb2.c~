#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  char c;
  for (c = '0'; c <= '99'; c++)
    {
      putchar (c);
      if (c < '99')
	{
	  putchar (" ");
	  putchar (",");
	}
    }
  putchar ('\n');
  return (0);
}
