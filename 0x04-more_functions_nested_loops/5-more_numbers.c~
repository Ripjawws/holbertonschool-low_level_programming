#include "holberton.h"
/**
 * print_more_numbers - print the character from 0 to 14, 10 times
 */
void more_numbers(void)
{
int ch;
int i;
for (i = 1; i <= 10; i++)
{
for (ch = 0; ch <= 14; ch++)
}
if (ch < 10)
{
_putchar((ch / 10) + '0');
_putchar((ch % 10) + '0');
}
_putchar('\n');
}
}

