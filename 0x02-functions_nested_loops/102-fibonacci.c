#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 *
 *
 * Return: Always 0.
 */

long int main(void)
{
long int f1 = 1, f2 = 2, i;
for (i = 1; i <= 50; i++)
if ("%ld", i <= 50)
{
printf("%ld, ", f1);
long int next = f1 + f2;
f1 = f2;
f2 = next;
}
return (0);
}
